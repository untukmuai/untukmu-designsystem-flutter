import 'package:flutter/material.dart';
import 'package:untukmu_flutter_design_system/src/common/sizing.dart';

class DLSRadius {
  static final BorderRadius radius0 = BorderRadius.circular(DLSSizing.radius0);
  static final BorderRadius radius4 = BorderRadius.circular(DLSSizing.radius4);
  static final BorderRadius radius8 = BorderRadius.circular(DLSSizing.radius8);
  static final BorderRadius radius10 =
      BorderRadius.circular(DLSSizing.radius10);
  static final BorderRadius radius12 =
      BorderRadius.circular(DLSSizing.radius12);
  static final BorderRadius radius16 =
      BorderRadius.circular(DLSSizing.radius16);
  static final BorderRadius radius20 =
      BorderRadius.circular(DLSSizing.radius24);
  static final BorderRadius radius24 =
      BorderRadius.circular(DLSSizing.radius24);
  static final BorderRadius radius32 =
      BorderRadius.circular(DLSSizing.radius32);
  static final BorderRadius radius40 =
      BorderRadius.circular(DLSSizing.radius40);
  static final BorderRadius radiusFull =
      BorderRadius.circular(DLSSizing.radiusFull);
}
